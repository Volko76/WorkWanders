<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WorkWonders - Panel Recruteur</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/core_pages.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
    <style>
        body {
            padding-top: 4rem;
        }

        .to_left p {
            text-align: left;
        }
    </style>
</head>

<body data-bs-theme="dark">
    <%- include("./header.ejs") %>
        <main>
            <div class="container text-center">
                <h1>Page Recruteur</h1>
                <div class="container d-flex flex-col">
                    <div class="container">
                        <div class="container border border-white rounded-4 m-2 to_left">
                            <h2>Mon entreprise :</h2>
                            <p>Nom : <%= organization.name %>
                            </p>
                            <p>Localisation : <%= organization.head_office %>
                            </p>
                            <a href="/organizations/editOrganization" class="btn btn-light mb-3">Editer
                                l'organisation</a>
                        </div>
                        <div class="container border border-white rounded-4 m-2 to_left">
                            <h2>Mon Profil :</h2>
                            <p>Compte actif:
                                <% if (user.isActive) { %>
                                    <span class="text-success">&#9679;</span>
                                    <% } else { %>
                                        <span class="text-danger">&#9679;</span>
                                        <% } %>
                            </p>
                            <p>Prénom: <%= user.first_name %>
                            </p>
                            <p>Nom: <%= user.name %>
                            </p>
                            <p>Date de création du compte: <%= user.creation_date %>
                            </p>
                            <p>Téléphone: <%= user.phone %>
                            </p>
                            <p>Email: <%= user.email %>
                            </p>
                            <a href="/users/editProfile" class="btn btn-light mb-3">Edit Profile</a>
                            <a href="/users/changePassword" class="btn btn-warning mb-3">Change Password</a>
                        </div>
                    </div>
                    <div class="container border border-white rounded-4 m-2">
                        <h2>Mes offres</h2>
                        <% for (var i=0; i < jobOffers.length; i++) { %>
                            <div class="d-flex m-3 flex-row align-items-center">
                                <div
                                    class="flex-grow-1 d-flex flex-row m-2 border border-white rounded-4 align-items-center">
                                    <p class="flex-grow-1">
                                        <%= jobOffers[i].title %>
                                    </p>
                                </div>
                                <button class="btn btn-info me-2" title="Explore <%= jobOffers[i].title %>"
                                    onclick="location.href='/joboffers/explore?id=<%= jobOffers[i].id %>'">
                                    <i class="material-icons">search</i>
                                </button>
                                <button class="btn btn-light me-2" title="Edit <%= jobOffers[i].title %>"
                                    onclick="location.href='/joboffers/edit/<%= jobOffers[i].id %>'">
                                    <i class="material-icons">edit</i>
                                </button>
                                <button class="btn btn-danger me-2" title="Remove <%= jobOffers[i].title %>"
                                    onclick="location.href='/joboffers/remove/<%= jobOffers[i].id %>'">
                                    <i class="material-icons">delete</i>
                                </button>
                                <button class="btn btn-info" title="Browse candidatures <%= jobOffers[i].title %>"
                                    onclick="location.href='/joboffers/browse_candidatures/<%= jobOffers[i].id %>'">
                                    <i class="material-icons">list</i>
                                </button>
                            </div>
                            <% } %>
                                <a href="/joboffers/create" class="btn btn-light">Créer une offre d'emploi</a>
                    </div>
                </div>
            </div>
        </main>
        <%- include("./footer.ejs") %>
</body>

</html>