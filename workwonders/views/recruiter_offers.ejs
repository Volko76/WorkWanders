<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WorkWonders - Panel Recruteur</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
    <link rel="stylesheet" href="css/core_pages.css">
    <style>
        body {
            padding-top: 4rem;
            color: #ffffff;
            /* animation: fadeIn 2s ease-in-out; */
        }

        main {
            animation: fadeInUp 1s ease-in-out;
        }

        .btn-success {
            background-color: #28a745;
            border: none;
            padding: 0.75rem 1.5rem;
            border-radius: 50px;
            transition: background-color 0.3s ease;
        }

        .btn-success:hover {
            background-color: #218838;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }

        @keyframes fadeInUp {
            from {
                transform: translateY(20px);
                opacity: 0;
            }
            to {
                transform: translateY(0);
                opacity: 1;
            }
        }

        @keyframes slideIn {
            from {
                transform: translateX(-20px);
                opacity: 0;
            }
            to {
                transform: translateX(0);
                opacity: 1;
            }
        }
    </style>
</head>

<body data-bs-theme="dark">
    <%- include("./header.ejs")%>

    <main>
        <div class="container text-center">
            <h1>
                <%= jobOffers[0].title %>
            </h1>
            <% if (user) { %>
                <% if (user.role==='candidate' ) { %>
                    <a href="/jobOffers/applyForm/<%= jobOffers[0].id %>" class="btn btn-success">Postuler</a>
                <% } else { %>
                    <p>Vous devez être chercheur pour postuler à cette offre.</p>
                <% }} else { %>
                    <a href="/jobOffers/applyForm/<%= jobOffers[0].id %>" class="btn btn-success">Postuler</a>
            <% } %>

            <div class="container d-flex flex-column">
                <div class="container border border-white rounded-4 m-2">
                    <div style="text-align: left !important;">
                        <h2>Nom de l'offre</h2>
                        <p><%= jobOffers[0].title %></p>
                        <h2>État</h2>
                        <p><%= jobOffers[0].state %></p>
                        <h2>Date de validité</h2>
                        <p><%= jobOffers[0].validity_date.toISOString().slice(0, 10) %></p>
                        <h2>Documents requis</h2>
                        <p><%= jobOffers[0].required_documents_list %></p>
                        <h2>Nombre de documents requis</h2>
                        <p><%= jobOffers[0].nb_required_documents %></p>
                        <h2>Statut du poste</h2>
                        <p><%= jobOffers[0].position_status %></p>
                        <h2>Responsable hiérarchique</h2>
                        <p><%= jobOffers[0].line_manager %></p>
                        <h2>Type de profession</h2>
                        <p><%= jobOffers[0].profession_type %></p>
                        <h2>Lieu de la mission</h2>
                        <p><%= jobOffers[0].mission_location %></p>
                        <h2>Cadence</h2>
                        <p><%= jobOffers[0].pace %></p>
                        <h2>Fourchette de salaire</h2>
                        <p><%= jobOffers[0].salary_range %></p>
                        <h2>Description</h2>
                        <p><%= jobOffers[0].description %></p>
                        <h2>SIREN de l'organisation</h2>
                        <p><%= jobOffers[0].organization_siren %></p>
                    </div>
                </div>
            </div>
        </div>
    </main>
    <%- include("./footer.ejs")%>

</body>

</html>
